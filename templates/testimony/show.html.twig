{% extends 'base.html.twig' %}

{% block title %}Testimony{% endblock %}

{% block body %}
    <h1>Testimony</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ testimony.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ testimony.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ testimony.age }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ testimony.content }}</td>
            </tr>
            <tr>
                <th>Validate</th>
                <td>{{ testimony.validate ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_testimony_index') }}">back to list</a>

    <a href="{{ path('app_testimony_edit', {'id': testimony.id}) }}">edit</a>

    {{ include('testimony/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Rarety{% endblock %}

{% block body %}
    <h1>Rarety</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rarety.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ rarety.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rarety_index') }}">back to list</a>

    <a href="{{ path('app_rarety_edit', {'id': rarety.id}) }}">edit</a>

    {{ include('rarety/_delete_form.html.twig') }}
{% endblock %}
